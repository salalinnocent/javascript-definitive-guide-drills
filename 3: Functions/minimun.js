// Write a function min that takes two arguments and return the minimun value
//let value = 0;
function minimun(x, y) {
    if (x < y) {
        return x;
    } else if (y < x) {
        return y;
    }
}
console.log(minimun('Enter any value (x, y)'))